# ============================================================================
# 文件闪传系统 - 环境变量配置示例
# ============================================================================
# 
# 使用方法：
# 1. 复制此文件为 .env: cp .env.example .env
# 2. 根据实际情况修改 .env 中的配置值
# 3. 注意：.env 文件包含敏感信息，不要提交到版本控制系统
# 
# ============================================================================

# ----------------------------------------------------------------------------
# 数据库配置
# ----------------------------------------------------------------------------
# MySQL 数据库连接信息
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=quick_share_datagrip

# ----------------------------------------------------------------------------
# Redis 配置（可选，用于缓存和会话存储）
# ----------------------------------------------------------------------------
# Redis 服务器地址
REDIS_HOST=localhost
# Redis 服务器端口
REDIS_PORT=6379
# Redis 密码（如果设置了密码）
REDIS_PASSWORD=
# Redis 数据库编号（0-15）
REDIS_DB=0
# 是否启用 Redis（true/false）
# 如果设置为 false，将使用内存字典作为缓存（不持久化）
REDIS_ENABLED=false

# ----------------------------------------------------------------------------
# JWT 配置
# ----------------------------------------------------------------------------
# JWT 密钥（用于生成和验证访问令牌）
# 生产环境务必修改为随机高熵值，建议使用 secrets.token_urlsafe(32) 生成
JWT_SECRET_KEY=change-me-in-production

# ----------------------------------------------------------------------------
# 去重指纹 Pepper（服务器端秘密）
# ----------------------------------------------------------------------------
# 用途：从 (user_id + 明文文件哈希) 派生去重指纹（dedupe fingerprint）
# 
# 安全说明：
# - 这是服务器端秘密，用于保护文件去重指纹不被外部推断
# - 即使数据库泄露，攻击者也无法通过指纹反推明文文件哈希
# - 生产环境务必配置为随机高熵值（建议 32 字节以上）
# - 不要泄露到日志、前端或版本控制系统
# 
# 生成方法（Python）：
#   import secrets
#   print(secrets.token_urlsafe(32))
# 
# 生成方法（命令行）：
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
# 
# 说明：
# - 如果未配置，将回退使用 JWT_SECRET_KEY（仍是服务器端秘密）
# - 一旦设置并开始使用，不要随意更改（会导致无法匹配已有文件的指纹）
DEDUPE_PEPPER=
